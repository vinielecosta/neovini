require("kanagawa").setup({
    colors = {
        theme = {
            all = {
                ui = {
                    bg_gutter = "none" -- Removing the horrible gutter bg color
                }
            }
        }
    }
})  
